import { Category, Brand } from '@/types';

// These values must be loaded from Firestore / the Node backend and never embedded in the frontend.
export const DEFAULT_CATEGORIES: Category[] = [];
export const DEFAULT_BRANDS: Brand[] = [];

